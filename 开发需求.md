
CertKeeper 证书管家工具
实现一个使用acme.sh 统一配置签发证书的服务。服务要解决的问题：不用在每个位置安装和配置 acme.sh 来申请签发和续签证书，要实现一个地方统一配置，客户端直接调用服务端生成证书和续签证书。

暂时的构想：
1. 工具氛围服务端和客户端。
2. 服务端使用go开发，使用 acme.sh 申请证书，服务端最终暴露一个服务，给客户端使用
  - 需要考虑暴露服务，需要能指定认证参数，服务需要可以管理认证参数
  - 服务端可以配置生成的证书参数规则支持：
    方式1. 在服务端配置，后面再客户端直接申请或续期证书，不需要配置；
    方式2. 服务端提供服务不配置，在客户端提供简易参数，调用服务端来设置参数配置，然后申请和续签证书
  - 服务端要支持查看所有证书配置和证书的有效时间，客户端申请证书的日志
  - 服务端需要知道有哪些服务端连接
  - 服务端最终打包为 docker image 使用 docker 运行。相关配置和持久化文件通过挂载到主机


3. 客户端需要一个命令运行，需要可以指定服务端的地址和认证参数，没有配置服务端的情况下不可以单独使用
- 可以手动执行，也可以通过crontab 配置定时执行。可以设置调用日志，指定日子位置

代码开发位置 ./extra-tools/cert-keeper

可以参考：
https://github.com/julydate/acmeDeliver  服务端
https://github.com/julydate/acmeDeliver/tree/master#client 客户端

